# Local definition drop-in / overrides

Custom or override `*.yml` definitions. These take precedence over the vendored
snapshot (same `id` wins): the one Prowlarr-only tracker, your own defs, hotfixes.
